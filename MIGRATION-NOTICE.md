# Migration Notice — 2026-03-15

## What happened

This repo (`mdc159/01`) has been forked to a new GitHub account: **`cbassist/Kit`**

All future development will happen on `cbassist/Kit`. This local clone (`~/projects/01/`) will be replaced with a fresh clone of Kit once the migration is complete.

## What's changing

- **GitHub account**: `mdc159` → `cbassist`
- **Repo name**: `01` → `Kit`
- **Remote URL**: will change from `mdc159/01` to `cbassist/Kit`

## What you need to know

- Any uncommitted work in this directory should be committed and pushed to `mdc159/01` before the switchover
- Once the migration happens, this directory gets deleted and re-cloned from `cbassist/Kit`
- The `gh` CLI on this machine has both accounts configured — use `gh auth switch -u cbassist` when working on Kit
- Nothing about the code itself is changing — just ownership and where it lives on GitHub

## Timeline

Happening now (2026-03-15). Mike is migrating this Mac Mini to his son Sebastian. Repos specific to this machine are moving to the `cbassist` account.
